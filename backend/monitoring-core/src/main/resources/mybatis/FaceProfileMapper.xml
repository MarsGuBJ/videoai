<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.videoai.monitoring.core.dao.FaceProfileDao">

    <select id="selectAllOrdered" resultType="com.videoai.monitoring.core.entity.FaceProfileEntity">
        SELECT * FROM face_profiles ORDER BY created_at DESC
    </select>

    <update id="updateWithPhoto" parameterType="com.videoai.monitoring.core.entity.FaceProfileEntity">
        UPDATE face_profiles
        SET name = #{name}, description = #{description}, photo_path = #{photoPath},
            embedding = #{embedding}, updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="updateWithoutPhoto" parameterType="com.videoai.monitoring.core.entity.FaceProfileEntity">
        UPDATE face_profiles
        SET name = #{name}, description = #{description}, updated_at = now()
        WHERE id = #{id}
    </update>

</mapper>
