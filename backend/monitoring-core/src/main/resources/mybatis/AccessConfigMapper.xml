<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.videoai.monitoring.core.dao.AccessConfigDao">

    <select id="selectConfig" resultType="java.lang.String">
        SELECT config FROM access_config WHERE protocol = #{protocol}
    </select>

    <insert id="upsertConfig">
        INSERT INTO access_config (protocol, config, updated_at)
        VALUES (#{protocol}, CAST(#{config} AS jsonb), now())
        ON CONFLICT (protocol) DO UPDATE
        SET config = EXCLUDED.config, updated_at = now()
    </insert>

</mapper>
